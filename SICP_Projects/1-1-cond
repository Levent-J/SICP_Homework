#lang racket
(define (abs x)
  (cond
    ((> x 0) x)
    ((= x 0) 0)
    ((< x 0) (- x))
    )
  )
(define (abs2 x)
  (cond
    ((< x 0) (- x))
    (else x)
    )
  )

(define (abs3 x)
  (if (< x 0) (- x) x)
  )
(abs -1)
(abs2 -1)
(abs3 -1)

(and (> 1 5) (> 5 1))
(and (> 6 5) (> 5 1))
10